<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:lang="http://www.springframework.org/schema/lang"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	                    http://www.springframework.org/schema/lang http://www.springframework.org/schema/lang/spring-lang-3.0.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">
    <!-- Context source for establishing connection to the LDAP server -->

    <import resource="propertyFileConfigurer.xml" />

    <bean id="dirContextValidator"
          class="org.springframework.ldap.pool.validation.DefaultDirContextValidator" />

    <bean id="contextSourceTarget" class="org.springframework.ldap.core.support.LdapContextSource">
        <property name="url" value="${thekey.ldap.url}" />
        <property name="userDn" value="${thekey.ldap.userdn}" />
        <property name="password" value="${thekey.ldap.password}" />
        <property name="base" value="${thekey.ldap.base}" />
        <property name="pooled" value="false"/>
    </bean>

    <bean id="contextSourceTarget.merge" class="org.springframework.ldap.core.support.LdapContextSource">
        <property name="url" value="${thekey.ldap.url.merge}" />
        <property name="userDn" value="${thekey.ldap.userdn.merge}" />
        <property name="password" value="${thekey.ldap.password.merge}" />
        <property name="base" value="${thekey.ldap.base.merge}" />
        <property name="pooled" value="false"/>
    </bean>

    <bean id="ldap.pooled.contextSource"
          class="org.springframework.ldap.pool.factory.MutablePoolingContextSource">
        <property name="contextSource" ref="contextSourceTarget" />
        <property name="dirContextValidator" ref="dirContextValidator" />
    </bean>

    <bean id="ldap.pooled.contextSource.merge"
          class="org.springframework.ldap.pool.factory.MutablePoolingContextSource">
        <property name="contextSource" ref="contextSourceTarget.merge" />
        <property name="dirContextValidator" ref="dirContextValidator" />
    </bean>

    <bean id="ldap.pooled.contextSource.transaction"
          class="org.springframework.ldap.transaction.compensating.manager.TransactionAwareContextSourceProxy" >
        <constructor-arg ref="ldap.pooled.contextSource"/>
    </bean>

    <bean id="ldap.pooled.contextSource.transaction.merge"
          class="org.springframework.ldap.transaction.compensating.manager.TransactionAwareContextSourceProxy" >
        <constructor-arg ref="ldap.pooled.contextSource.merge"/>
    </bean>

    <!-- Template for performing DAO execution -->
    <bean id="ldap.template" class="org.springframework.ldap.core.LdapTemplate">
        <property name="contextSource" ref="ldap.pooled.contextSource.transaction"/>
    </bean>

    <!-- Template for merged accounts -->
    <bean id="ldap.template.merge" class="org.springframework.ldap.core.LdapTemplate">
        <property name="contextSource" ref="ldap.pooled.contextSource.merge"/>
    </bean>

    <!-- LDAP Binders -->
    <bean id="ldap.bind.gcxUser" class="org.ccci.gcx.idm.core.persist.ldap.bind.impl.GcxUserAttributeBind" />

    <!-- DAO Implementations -->
    <bean id="ldap.abstractDao" abstract="true">
        <property name="maxSearchResults" value="1000" />
    </bean>

    <bean id="ldap.gcxUserDao" class="org.ccci.gcx.idm.core.persist.ldap.GcxUserDaoImpl" parent="ldap.abstractDao">
        <property name="ldapTemplate" ref="ldap.template" />
        <property name="attributeBind" ref="ldap.bind.gcxUser" />
        <property name="modelDN" value="${thekey.ldap.dn.gcxuser}" />
    </bean>

    <bean id="ldap.gcxUserDao.merge" class="org.ccci.gcx.idm.core.persist.ldap.GcxUserDaoImpl"
          parent="ldap.abstractDao">
        <property name="ldapTemplate" ref="ldap.template.merge" />
        <property name="attributeBind" ref="ldap.bind.gcxUser" />
        <property name="modelDN" value="${thekey.ldap.dn.gcxuser}" />
    </bean>

</beans>
